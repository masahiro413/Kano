# CLAUDE.md

このファイルは、このリポジトリでClaude Codeが作業する際のガイドラインです。

## プロジェクト概要

ベトナム語会話スクール向けの業務・学習Webアプリケーション。詳細な仕様は **[SPEC.md](./SPEC.md)** を、実装タスクの分解は **[TASKS.md](./TASKS.md)** を参照すること。作業を始める前に必ずこの2ファイルに目を通す。

アプリは大きく2つの独立したセクションを持つ:
1. **タスク管理**: 生徒・講師・管理者それぞれが使う汎用ToDo(追加・完了・削除)
2. **学習機能**: 問題集・単語帳(SRS)・リスニング・AI発音採点

両者はログイン・ユーザー管理のみ共有し、機能・データは連携させない。

## 技術スタック

- フロントエンド: React + TypeScript + Vite
- バックエンド: Firebase(Firestore / Authentication / Storage / Hosting)— 自前サーバーは持たない
- 外部API: Azure AI Speech(Pronunciation Assessment / TTS)、または Google Cloud TTS
- i18n: 日本語/ベトナム語切り替え対応

将来的に別の開発者へ引き継ぐ前提のプロジェクトのため、**ニッチな技術選定を避け、一般的で読みやすい実装を優先する**こと。

## 開発時の重要な前提

- **タスク管理のデータはユーザーごとに完全に独立**する。共有タスク一覧は存在しないため、Firestoreクエリ・セキュリティルールは必ず所有者(uid)でスコープすること。
- **講師と管理者は権限上区別しない**(1つの `staff` ロールとして扱う)。「担当生徒」のような割り当て概念は実装しない。
- **生徒登録は承認制**。サインアップ直後の生徒は「承認待ち」状態であり、承認されるまで機能を一切使わせない。承認/拒否はstaffなら誰でもボタン一つで行える(理由入力や通知は不要)。
- **「コース」と「レベル」は独立した2軸**。コースは科目区分(日常会話/ビジネス/発音強化など)、レベルは習熟度(初級/中級/上級)。両方を混同した実装をしないこと。
- レベルはプレースメントテストで初期設定され、以降は問題集の成績で自動的に昇降級する。生徒や講師が手動でレベルを変更する機能は現スコープにない。
- 発音採点は **Azure AI Speech の Pronunciation Assessment** を利用する前提で設計する(ベトナム語 `vi-VN` は公式対応言語)。単純な音声類似度スコアではなく、音素レベル(正確性・流暢さ・完全性)の採点を行う。
- **発音採点はAzureの素点をそのまま使わない。** Azureの`vi-VN`は方言を区別しない単一基準のため、その音素差分結果に対して**南部(サイゴン)方言許容ルールによる後処理レイヤー**を通す設計とする(SPEC.md §4.5)。生徒ごとに「評価対象方言(北部標準/南部サイゴン)」を切り替えられる。ルールテーブルは子音・母音韻母・声調の3種類に分かれ、母音韻母のみユーザー提供資料で初期データがあるが、子音・声調は未整備(SPEC.md §8)。このロジックを実装する際は、資料が揃っていない部分を勝手に推測・捏造しないこと。
- 外部AI API(TTS・音声認識)は利用量課金のクラウドサービス。現時点で厳密な利用回数制限の実装は必須ではないが、将来追加しやすいようログ・呼び出し箇所は一箇所にまとめておく。

## コーディング規約

- コメントは最小限。WHYが自明でない箇所にのみ短く記述する。
- 未使用のスコープ拡張(将来の要件を先回りした抽象化)は避ける。SPEC.mdに明記された範囲を実装する。
- SPEC.md §8「未決事項」に該当する仕様は、実装時に仮決めせず、ユーザーに確認してから進める。

## ファイル構成の注意

このリポジトリには元々Shopifyテーマの雛形(`layout/` `sections/` `snippets/` `templates/` 等)が存在しているが、本アプリの実装とは無関係。新規実装は別ディレクトリ(例: `src/`)を切って進めること。
