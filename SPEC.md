# SPEC.md — ベトナム語会話スクール向け業務・学習アプリ 仕様書

## 0. 概要

ベトナム語会話スクールの運営を支えるWebアプリケーション。PCブラウザを主軸としつつスマートフォンでも利用できるレスポンシブ設計とし、以下2つの独立した機能セクションを1つのアプリ内に持つ。

1. **タスク管理機能** — 生徒・講師・管理者それぞれが使う汎用ToDo管理
2. **学習機能** — 問題集・単語帳・リスニング・発音練習

両セクションはログイン・ユーザー管理を共有するが、データやUIフローは独立している(例:「今日の単語帳」がタスクの1項目になるような連携は行わない)。

対象ユーザーはベトナム語話者である可能性があるため、UIは**日本語/ベトナム語の切り替え(i18n)** に対応する。

---

## 1. ユーザーロールと権限

| ロール | 概要 |
|---|---|
| 生徒 (student) | タスク管理・学習機能を利用する。自分のタスク一覧・自分の学習進捗のみ閲覧可能。 |
| 講師/管理者 (staff) | 現時点ではロールを区別しない(小規模運用のため)。生徒の承認、コンテンツ(問題集・単語帳・リスニング教材)の作成/編集、全生徒の進捗ダッシュボード閲覧が可能。 |

- 「担当生徒」のような割り当て概念は**持たない**。staffロールを持つ全員が全生徒を閲覧・管理できる。
- 「講師」と「管理者」を分離する権限モデルは現バージョンのスコープ外(将来拡張の余地として設計はシンプルに保つ)。

### タスク管理のデータ分離
- タスク一覧は**ユーザー(ロール問わず)ごとに完全に独立**する。生徒Aのタスクは生徒Aしか見えず、staffのタスクもそのstaff本人専用。全員共有のタスク一覧は存在しない。

---

## 2. 認証・生徒登録フロー

### 2.1 認証方式
- 本格的なログイン(メールアドレス + パスワード)。Firebase Authentication(Email/Password)を採用。
- パスワードリセットはFirebase Authentication標準機能を利用。

### 2.2 生徒登録(セルフサインアップ + 承認制)
1. 生徒が登録フォームから以下を入力してアカウントを作成する:
   - 氏名・メールアドレス・パスワード(必須)
   - 連絡先(電話番号・住所など)
   - 受講コースの選択(§4.2参照)
   - 表示言語の初期設定(日本語/ベトナム語)
2. アカウント作成直後は **「承認待ち」状態**。ログイン自体は可能だが、「承認待ちです」という画面のみが表示され、他の機能は一切利用できない。
3. staff側の管理画面に「承認待ち一覧」が表示され、誰でも(講師=管理者のため権限区別なし)ボタン一つで承認/拒否できる。理由入力や通知機能は持たない(シンプル運用)。
4. 承認されると、生徒は**プレースメントテスト**(レベル判定用の問題集)を受験する。
5. プレースメントテストの結果に基づき初期レベル(初級/中級/上級)が決定される。以降は通常の問題集の成績に応じてレベルが自動的に昇級/降級する。

### 2.3 staffアカウント
- staffアカウントはセルフサインアップの対象外。運用上、既存staffが直接発行する(詳細な発行UIはTASKS.mdでMVP後の課題とする)。

---

## 3. タスク管理機能

- 操作: 追加・完了・削除
- タスク項目のフィールド:
  - タイトル(必須)
  - 優先度(高/中/低)
- 学習機能との自動連携は行わない(独立したセクション)。

---

## 4. 学習機能

### 4.1 コンテンツの構成要素
- **問題集**: 出題形式は以下を複数サポート
  - 選択式(4択など)
  - 穴埋め・記述式
  - リスニング問題(音声を聞いて選択式/記述式で回答)
  - 発音問題(指定文を発声し、AIが採点)
- **単語帳**: 間隔反復方式(SRS)。苦手な単語ほど高頻度で出題する。
- **リスニング教材**: ネイティブ講師の録音とAI音声合成(TTS)を状況に応じて併用。
- **発音練習**: AI音声認識による自動採点(§4.5参照)。

### 4.2 コースとレベルの2軸モデル
学習コンテンツは **「コース」** と **「レベル」** という独立した2つの軸でタグ付けされる。

- **コース**: 科目区分。例)日常会話コース / ビジネスコース / 発音強化コースなど。生徒が登録時に選択する。
- **レベル**: 習熟度。初級/中級/上級の3段階。プレースメントテストで初期設定され、以降は問題集の正答率に応じてシステムが自動的に昇級・降級判定を行う(具体的な閾値ロジックはTASKS.mdで詳細設計する)。

問題集・単語帳・リスニング教材は、この「コース×レベル」の組み合わせで公開範囲が決まる。

### 4.3 コンテンツ管理(CMS)
- staff(講師・管理者)がアプリ内の管理画面から、問題・単語・リスニング教材を自由に追加・編集できる。
- コンテンツ作成時に「コース」「レベル」のタグ付けを行う。
- リスニング教材の音声は、(a) staffが録音した音声ファイルをアップロードする方式、(b) テキストを入力しTTSで自動生成する方式の両方をサポートする。

### 4.4 進捗ダッシュボード
- staff向けに、生徒ごとの学習進捗(正答率、発音スコアの履歴など)を一覧できるダッシュボードを提供する。
- 生徒本人も自分自身の履歴・スコア推移を閲覧できる。

### 4.5 発音採点の技術方式(北部/南部方言対応)

#### 4.5.1 ベースエンジン
- **Azure AI Speech の Pronunciation Assessment 機能**を採用する。
- ベトナム語(`vi-VN`)はPronunciation Assessmentの公式対応言語(34言語)に含まれることを確認済み([Microsoft公式ソース](https://raw.githubusercontent.com/MicrosoftDocs/azure-ai-docs/main/articles/ai-services/speech-service/includes/language-support/pronunciation-assessment.md))。
- 採点粒度: **音素レベル**(正確性 Accuracy・流暢さ Fluency・完全性 Completeness)を `Granularity: Phoneme` で取得する。
- 料金目安: リアルタイム音声認識ベースで約$1.32/時間相当。小規模運用(生徒数十名規模)では月額数千円〜$30程度に収まる想定。厳密な利用回数制限はMVPでは実装しないが、将来の拡張余地として設計しておく。

#### 4.5.2 方言判定を上乗せする理由
- Azure Pronunciation Assessmentの`vi-VN`ロケールは方言選択ができない単一基準であり、内部の期待発音(基準音素列)がどちらの方言に基づくかも非公開。
- そのため、南部(サイゴン)方言を正しく発音した生徒が、北部標準との音素差分だけを理由に不当に減点される可能性がある(例: 語末の -anh/-ach が南部では -an/-ak に、声調が5つに統合される、など)。
- 開発者自身がホーチミン市の大学(Trường Đại học Khoa học Xã hội và Nhân văn)に1年間留学し、北部・南部双方の発音に精通しているという背景があり、**Azureの汎用採点結果に、南部方言の許容ルールを適用する後処理レイヤーを本格実装する**方針を採用する。

#### 4.5.3 アーキテクチャ
1. Azure Pronunciation Assessmentから、音素単位の「期待音素 vs 認識音素」の差分結果を取得する(北部標準基準)。
2. 「南部方言許容ルールテーブル」を参照し、Azureが誤りと判定した音素差分が、南部方言として正当な異音対応(regional variant)に該当する場合は減点しない、または軽減する。
3. 生徒プロフィールに **「評価対象方言」(北部標準 / 南部サイゴン)** を設定する項目を持たせ、採点時にどちらのルールセットを適用するか切り替える。
4. 許容ルール適用後のスコアを最終スコアとして記録し、講師ダッシュボードには「Azure素点」と「方言補正後スコア」の両方を表示できるようにする(補正の妥当性を講師が検証できるようにするため)。

#### 4.5.4 南部方言許容ルールテーブルの構成(3種類)
発音は「子音・母音・イントネーション」の3要素で評価する、という要件に対応し、ルールテーブルを3種類に分けて整備する。

| # | 種別 | 内容 | データ整備状況 |
|---|---|---|---|
| 1 | 語頭子音・介音(âm đầu, âm đệm) | 標準語とNam Bộ(南部)方言の音素対応 | ユーザー提供資料「Bảng đối chiếu âm đầu và âm đệm」(付録3・30頁)を初期データとして取得済み。§4.5.4-a参照。デジタル化・構造化データへの変換が必要 |
| 2 | 韻母(母音+末子音) | 母音の融合・末子音の変化(例: -anh/-ach→-an/-ak) | ユーザー提供資料「Vần mẫu tiếng Hà Nội」(付録4・表1、北部/標準語基準)と「Vần mẫu tiếng Sài Gòn」(付録4・表2、南部)が**対になった資料として両方取得済み**。同一の母音×末子音マス目構成のため、表1と表2をセル単位で突き合わせることで許容ルールを導出できる。§4.5.4-b参照。**ただし写真からの読み取りによる転記誤りのリスクがあるため、全セルの正式なデジタル化・検証は別途慎重に行う** |
| 3 | 声調(トーン) | 南部5声調(hỏi/ngã統合)への対応、声調の融合パターン | 標準ベトナム語6声調の一般理論(高調/低調=声調1,3,5/2,4,6、平坦/破裂/非破裂の分類)は資料取得済みだが、これは北部・南部の対応を示すものではない。**北部⇔南部の声調融合パターンを示す対照表は未取得** |

3種のうち語頭子音・韻母の2種は初期資料が揃った。声調の対応表が揃うまでは、方言判定ロジックは語頭子音・韻母の範囲のみ先行実装し、声調は資料が整い次第、段階的に追加する。

#### 4.5.4-a 語頭子音・介音の対照表(初期データ、要デジタル化)

ユーザー提供資料「Bảng đối chiếu âm đầu và âm đệm」(付録3・30頁)より。標準ベトナム語(北部基準)とNam Bộ(南部)方言で発音が異なる語頭子音は以下の通り(同一のものは省略)。

| 綴り | 標準語の音素 | 南部(Nam Bộ)の音素 |
|---|---|---|
| v- | /v-/ | /j-/ (variant付き*) |
| d-, gi- | /z-/ | /j-/ (variant付き*) |
| tr- | /ʈ-/ | /t-/(北部方言での実際の発音。ch-と同音になる) |
| s | /ʂ-/ | /ʂ-/, /s-/(2つ目に*。北部方言ではx-と同音になる) |
| r- | /ʐ-/ | /ʐ-/, /ɣ-/(2つ目に*。北部方言ではd-/gi-と同音になる) |

上記以外(b-, m-, ph-, th-, t-, đ-, n-, x-, l-, ch-, nh-, c-/k-, qu-, ng-/ngh-, kh-, g-/gh-)は標準語と南部方言で同一の音素。

**tr-/r-の転記に関する訂正(解消済み)**: 一時、写真の読み取り精度に自信が持てず「tr-は両列とも/t-/に見える」「r-は反り舌の可能性がある」と留保していたが、ユーザー提供の別資料(活字組版の音韻論資料、"Green bold consonants: Not exist in Northern dialect" と明記)により解消した。この資料には「北部方言では ch-/c/とtr-/ʈ/が同音、d-,gi-/z/とr-/z/が同音、x-/s/とs-/ʂ/が同音になる」と明記されており、これは(a) 正書法上の標準音素体系(chuẩn)では tr-=/ʈ-/, r-=/ʐ-/, s-=/ʂ-/ が ch-=/c-/, d-/gi-=/z-/, x-=/s-/ とそれぞれ区別される独立音素であること、(b) **北部方言(Hà Nội)がこれらの反り舌音を非反り舌音に統合・簡略化している**ことを示している。これは19頁の記述(「反り舌音は中部・南部話者の発音特徴で、北部話者は/c,s,z/に置き換える」)と完全に整合する。したがって上表のtr-/r-行は当初の記録(標準語側が反り舌音)を正として復元した。

**未確定事項1**: 表中の「*」印の正確な凡例(南部方言内でのバリエーション・任意性を示すと推測されるが、原資料の凡例部分が写真に写っておらず未確認)。実装前にユーザーに確認する。

**未確定事項2(音韻論テキスト19頁との食い違い、部分的に解消)**: ユーザー提供の別資料(音韻論テキスト19頁)には「/ʈ, ʂ, ʐ/(tr-, s-, r-に相当する反り舌音)は中部・南部話者の発音特徴であり、北部話者はこれらを/c, s, z/に置き換えて発音する」との記述があり、上表と方向性が食い違って見えていた。

その後、より信頼性の高い一次資料(下記4.5.4-c、Hà Nội⇔Sài Gòn直接比較資料、SIL国際音声記号使用)を入手し、この資料では**南部(Sài Gòn)側が/ʂ-/(s-)を[s-]に、/v-/・/z-/(v-, d-, gi-)を[j-]に単純化する**方向で明記されている。これは上表(Nam Bộ列がより単純な音素になっている)と整合する。**tr-とr-の具体的な扱いは4.5.4-cの資料には直接記載がなく、19頁の記述との食い違いは完全には解消していない**が、s-, v-, d-, gi-については4.5.4-cを優先資料として採用する。tr-, r-の最終確認は今後の課題として残す(§8参照)。

#### 4.5.4-b 韻母(母音+末子音)の対照表(初期データ、要デジタル化・要検証)

ユーザー提供資料「Vần mẫu tiếng Hà Nội」(付録4・表1)と「Vần mẫu tiếng Sài Gòn」(付録4・表2)より。両表とも、母音の主要な核(i:/iə, e:/ɛ, a:/ɐ, ɔ:/o, u:/uə等)を行、末子音・末尾グライド(-∅, -w, -j, -m, -p, -n, -ŋ, -k)を列とする同一構成のマス目で、各セルに正書法上の綴りとIPA表記が対になって記載されている。

原資料には南部方言特有の融合(例: -anh/-ach系と-an/-ak系の統合、-in/-it系と-inh/-ich系の統合、-un/-ut系と-ôn/-ôc系の統合など)が手書きの丸囲み・矢印で個別に注釈されている。

**この資料の扱い方針**:
- 表1(北部/標準語)と表2(南部)は同一構成のため、本来はセル単位で1対1に突き合わせて許容ルールを機械的に導出できる。
- ただし、回転した写真からの手作業によるIPA記号の読み取りは誤読のリスクが高く、特に声調記号・下付き文字を含む音声記号は誤変換しやすい。**この場に記載されている内容を根拠に全セルを確定させることはせず**、実装フェーズで原資料(紙面またはスキャンPDF)を直接参照しながら、ユーザーまたは南部方言話者の確認を経て構造化データ化する。
- Azure Pronunciation Assessmentの内部基準発音がこの「Vần mẫu tiếng Hà Nội」表と完全に一致するとは限らない(Azureは方言基準を公表していないため)。本表は「北部標準として何を正しいとみなすか」の参照基準として使うものであり、Azureの内部モデルとの一致を保証するものではない点に留意する。

#### 4.5.4-c Tiếng Hà Nội ⇔ Tiếng Sài Gòn 直接比較資料(優先度の高い一次資料)

ユーザー提供資料(44〜48頁、南部方言の音韻的特徴を論じた学術文献の章。国際SIL言語学研究所の音声記号を使用し、著者自身が「Tiếng Hà Nội」と「Tiếng Sài Gòn」の発音を単語ごとに直接比較・記録したもの)より。付録3・4(§4.5.4-a, b)の手書き注釈付き対照表よりも出典が明確で信頼性が高いため、**内容が重複・矛盾する場合はこちらを優先する**。

**語頭子音の単純化(南部)**:
- /ʂ-/(綴り"s") → 南部では [s-] に単純化(正書法上の"s"が"x"と同じ発音になる)
- /v-/, /z-/(綴り"v", "d, gi") → 南部では [j-] に統合(正書法上の"v"と"d, gi"が同じ発音になる)

**韻母(母音+末子音)の変化(南部、確認できた具体例)**:
- 介音 /-w-/ が南部方言には存在しない。"-oa" 系は南部で [-ɔ:-](表記"o")寄りに、高齢層ではさらに [-a:-](表記"a")まで単純化する傾向。"-uơ-" 系は南部で [-ɯ-](表記"ư")寄りになる。例: đường thủy, loè loẹt, ra toà, môn toán, luân lý, hoa huệ, qua đường(各語についてTiếng Hà NộiとTiếng Sài GònのIPA比較あり)
- "-ênh", "-êm", "-êp" 系の母音 /-e-/ が南部でおおむね [-i-] に変化。例: con kênh, mênh mông, ban đêm, mắm nêm, nề nếp, sắp xếp
- "-ôm/-ôp" と "-ơm/-ơp" が南部で共通の韻母に合流し、さらに "-ăm/-ăp" と "-âm/-âp" も南部で近い発音([-am:], [-ap:] 系)に合流する
- "-iu" や "-ươu" 系が南部で [-ơw] 系に合流。例: về hưu, con hươu
- 南部方言話者は半母音を含む韻母における主母音 /-a-/ の長短を区別せず、すべて長母音として発音する。例: bàn tay, lỗ tai, con cháu, tô cháo
- 語末子音 /-n/と/-ng/、/-t/と/-k/ が南部方言で合流する(ただし /-ɲ/, /-c/ の異形は南部方言に存在しない)。例: đàn gà, lên đàng, cát bụi

**この資料の扱い方針**: 上記は原資料に明記された現象と具体例であり、§4.5.4-bの手書き注釈付き韻母表よりも出典の信頼性が高い。§4.5.4-bのセル単位デジタル化を行う際は、本資料の記述と矛盾する箇所がないか照合し、矛盾する場合は本資料を優先する。ただし本資料も全ての韻母パターンを網羅しているわけではなく、§4.5.4-bのカバー範囲(全母音×末子音マトリクス)を代替するものではない。

#### 4.5.5 精度検証
- 言語学的なルールテーブルであっても、実際の音声認識結果への適用は誤判定のリスクがある。南部方言のネイティブ話者による実音声でのレビュー・調整を実装後に行う。

### 4.6 音声コンテンツの調達
- TTS: Azure Neural TTS(`vi-VN-HoaiMyNeural`女性 / `vi-VN-NamMinhNeural`男性)を基本採用。無料枠(月50万文字、$16/100万文字)の範囲でほぼ収まる想定。
- 北部/南部の方言を明示的に選べるネイティブ音声が必要な場面(リスニング教材)では、ベトナム特化ベンダー **FPT.AI**(北部/中部/南部の声優音声を提供)の採用も検討中。ただし有料プランの従量課金レートが非公開で個別見積もり制のため、採用可否は別途調査の上で決定する(§8参照)。
- ネイティブ録音: staffが管理画面から音声ファイルをアップロードするフローを用意する。

---

## 5. 多言語対応 (i18n)
- UIの全ラベル・メッセージは日本語/ベトナム語の切り替えに対応する。
- 生徒は登録時・設定画面で表示言語を選択できる。
- 学習コンテンツ自体(問題文・単語など)の多言語化は別軸の話であり、i18nの対象はUIのみとする。

---

## 6. データ保存・技術方式

### 6.1 データ保存
- **Firebase(BaaS)を採用**し、自前のバックエンドサーバーは構築しない。
  - **Firestore**: タスク、生徒/staffプロフィール、問題集・単語帳・リスニング教材、学習履歴・スコアを保存。
  - **Firebase Authentication**: メール/パスワード認証。
  - **Firebase Storage**: ネイティブ録音の音声ファイル、生徒の発音録音(必要な場合)を保存。
  - **Firebase Hosting**: フロントエンドのホスティング。

### 6.2 データ共有範囲(Firestoreセキュリティルール設計の指針)
- タスクコレクションは所有ユーザーのみ read/write 可能。
- 学習コンテンツ(問題集・単語帳等)は、承認済み生徒であれば自分のコース×レベルに該当するものを read 可能、write はstaffのみ。
- 学習履歴・スコアは本人 + 全staffが read 可能。write は採点処理(Cloud Functions等)経由のみ。

### 6.3 フロントエンド技術スタック
- 「将来的に他の開発者に引き継ぐ可能性がある」という要件から、**汎用的で保守しやすい技術**を優先する。
- 推奨構成: **React + TypeScript + Vite**、状態管理はReact標準機能 + 必要に応じてContext/軽量ライブラリ、UIは一般的なコンポーネントライブラリ(例: MUIなど)を想定。ニッチなフレームワークは避ける。
- レスポンシブ対応必須(PC優先だがスマホでも即利用可能なレイアウト)。

---

## 7. 非機能要件

- **対応デバイス**: PCブラウザを基本としつつ、スマートフォンでも今すぐ利用できるレスポンシブUI。
- **セキュリティ**: 本格的な認証(メール+パスワード)、Firestoreセキュリティルールによるロール・所有者ベースのアクセス制御。
- **コスト**: 外部AI APIの利用はTTS・発音採点ともに小規模運用では低コスト(月数千円〜数十ドル規模)。利用回数の上限設定はMVPでは不要だが、将来的な拡張ポイントとして設計上考慮する。
- **保守性**: 引き継ぎを見据え、一般的な技術・命名規則・ドキュメント整備を優先する。

---

## 8. 未決事項・今後詰める点

以下はヒアリングの中でも「詳細は実装フェーズで検討」となった項目。TASKS.md側で設計タスクとして扱う。

1. レベル自動昇降級の具体的な判定ロジック(正答率の閾値、判定に使う問題数など)
2. 単語帳SRSアルゴリズムの詳細(SM-2方式を軸に検討)
3. staffアカウントの発行方法(招待リンク/管理画面からの直接作成など)
4. 発音採点APIの利用回数制限の要否(実際の運用コストを見て判断)
5. プレースメントテストの出題内容・問題数
6. 南部方言許容ルールテーブルのうち、**北部⇔南部の声調融合パターン対照表が未取得**(§4.5.4)。声調の一般理論(6声調の分類枠組み)は取得済みだが、方言間の対応表ではない
6a. 語頭子音対照表(§4.5.4-a)中の「*」印の凡例が未確認。実装前にユーザーに確認する
~~6b. 語頭子音対照表のtr-/r-の扱い~~ → **解消済み**。活字組版の音韻論資料("Green bold consonants: Not exist in Northern dialect")により、tr-=/ʈ-/, r-=/ʐ-/, s-=/ʂ-/ が標準音素体系の独立音素であり、北部方言がこれらをch-, d-/gi-, x-とそれぞれ統合する、という整合的な説明が得られた(§4.5.4-a参照)
9. §4.5.4-cで得られた韻母の変化パターン(介音/w/脱落、-ênh/-êm/-êp系の変化、-n/-ng・-t/-c語末の合流など)を、§4.5.4-bの全体マトリクスにどう統合するか(個別現象の記述と網羅的マトリクスの整合性を取る作業)
7. リスニング教材のTTSベンダーとしてFPT.AIを併用するか(§4.6)。有料プランの正確な料金が個別見積もり制のため、見積もり取得後に最終判断する
8. 方言判定ロジックの精度検証方法・実施体制(ネイティブ話者レビューの具体的な進め方)
